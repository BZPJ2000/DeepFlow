# DeepFlow 项目重构方案

## 文档版本
- 创建日期: 2026-01-17
- 项目状态: 重构规划阶段
- 目标: 构建自动感知函数、支持快速模型选择与训练的框架

---

## 第一部分：项目现状分析与清理计划

### 1.1 项目现状总结

**项目规模:**
- 总大小: 117 MB
- Python 文件: 346 个
- 主要语言: Python + Streamlit

**核心功能:**
- 基于 Streamlit 的深度学习实验管理平台
- 动态组件发现与加载系统
- 支持 4 大 AI 领域: NLP、计算机视觉、图神经网络、强化学习
- 实验配置、执行、结果可视化

**主要问题:**
1. 双应用结构混乱 (`app.py` vs `dl_experiment_manager/app.py`)
2. 临时文件和实验代码未清理 (`try/`, `.trae/`, `tmp/`)
3. 外部示例项目混入 (`streamlit_example-main/`)
4. 硬编码路径依赖外部框架
5. 缓存和日志文件累积
6. 缺乏统一的代码规范和文档

---

### 1.2 文件分类清单

#### ✅ 核心保留文件 (KEEP)

**应用入口:**
- `dl_experiment_manager/app.py` - 主应用 (10.5 KB, 功能完整)
- `dl_experiment_manager/` 整个目录 - 新版实验管理器

**页面模块 (7个):**
- `pages/1_任务方向.py` - 任务选择
- `pages/2_模型选择.py` - 模型选择与分析
- `pages/3_数据处理.py` - 数据处理配置
- `pages/4_训练设置.py` - 训练参数设置
- `pages/5_结果显示.py` - 结果可视化
- `pages/6_批量测试实验室.py` - 批量实验
- `pages/7_项目启动.py` - 项目启动器

**工具库 (18个模块):**
- `ulit/config.py` - 任务分类与配置
- `ulit/load_selected_class.py` - 动态类加载器
- `ulit/load_loss_function.py` - 损失函数加载
- `ulit/load_metric_function.py` - 评估指标加载
- `ulit/load_optimizer.py` - 优化器加载
- `ulit/load_dataset_class.py` - 数据集加载
- `ulit/model_utils.py` - 模型工具函数
- `ulit/split_dl_dataset.py` - 深度学习数据集分割
- `ulit/split_ml_dataset.py` - 机器学习数据集分割
- `ulit/settings_to_file.py` - 配置持久化
- `ulit/select_*.py` - 各类选择工具
- 其他工具模块

**组件库 (46 MB):**
- `models/` - 模型实现 (4个领域)
- `losses/` - 损失函数 (195 KB)
- `metrics/` - 评估指标 (35 KB)
- `optimizer/` - 优化器 (16 KB)
- `dataset/` - 数据集样例

**配置文件:**
- `requirements.txt` - 依赖清单
- `README.md` - 项目文档
- `env_safety_check.py` - 环境检查

---

#### ❌ 删除文件 (DELETE)

**冗余应用入口:**
- `app.py` - 简单占位符 (908 bytes, 被 dl_experiment_manager 替代)

**临时/实验目录:**
- `.trae/` - 文档笔记目录 (规划草稿)
- `try/` - 实验代码目录
- `tmp/` - 临时文件
- `streamlit_example-main/` - 外部示例项目

**IDE 配置:**
- `.idea/` - PyCharm 配置
- `__pycache__/` - Python 缓存 (所有位置)
- `*.pyc` - 编译文件

**生成文件 (运行时生成，不应提交):**
- `logs/` - 运行日志
- `cached_projects/` - 项目缓存
- `training_logs/` - 训练历史
- `saved_models/` - 模型检查点

---

#### 🔄 需要重构的文件 (REFACTOR)

**需要重命名/重组:**
- `ulit/` → `utils/` (标准命名)
- 中文页面文件名 → 英文或保持双语支持

**需要优化:**
- 硬编码路径 → 配置文件
- 重复代码 → 抽象为公共函数
- 缺少类型注解 → 添加类型提示
- 缺少文档字符串 → 添加 docstrings

---

### 1.3 清理执行计划

#### 阶段 1: 安全备份
```bash
# 创建备份分支
git checkout -b backup-before-refactor
git add .
git commit -m "Backup before major refactoring"

# 创建新的重构分支
git checkout -b refactor-project-structure
```

#### 阶段 2: 删除冗余文件
```bash
# 删除临时目录
rm -rf .trae/ try/ tmp/ streamlit_example-main/

# 删除 IDE 配置
rm -rf .idea/

# 删除 Python 缓存
find . -type d -name "__pycache__" -exec rm -rf {} +
find . -type f -name "*.pyc" -delete

# 删除冗余应用入口
rm app.py
```

#### 阶段 3: 更新 .gitignore
```gitignore
# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# IDE
.idea/
.vscode/
*.swp
*.swo

# 项目特定
logs/
cached_projects/
training_logs/
saved_models/
tmp/
.trae/
try/

# 数据文件
data/
*.h5
*.pth
*.ckpt

# 环境
venv/
env/
.env
```

#### 阶段 4: 清理生成文件
```bash
# 清空但保留目录结构
mkdir -p logs cached_projects training_logs saved_models
touch logs/.gitkeep cached_projects/.gitkeep
touch training_logs/.gitkeep saved_models/.gitkeep
```

---

### 1.4 清理后的预期结果

**删除内容:**
- 临时文件: ~5-10 MB
- IDE 配置: ~1-2 MB
- Python 缓存: ~2-5 MB
- 外部示例: ~10-20 MB

**保留内容:**
- 核心代码: ~50 MB
- 模型库: ~46 MB
- 文档配置: ~1 MB

**总体减少:** 约 20-40 MB，提升项目清晰度

---

## 下一步

完成清理后，将进入第二部分：**新架构设计**

